#include <stdio.h>

int main() {
    int leap_year;
    scanf("%d", &leap_year);
    if (x%4==0) ||(x%40==0) || (x%400==0) {
        printf("Leap Year");
    }
    else {
        printf("Not Leap Year");
    }
    return 0;
}